pipeline {
agent any stages {
        stage('Checkout GIT') {
            steps {
                echo 'Pulling'
                git branch: 'mahdi',
                credentialsId: 'cred',
                url: 'https://github.com/ibtchy/ProjetDevOps.git';
                
            }
        }
        }
